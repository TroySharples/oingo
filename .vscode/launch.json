{
    "version": "0.2.0",
    "configurations": [
        {
        "name": "Debug Sphere",
        "type": "cppdbg",
        "request": "launch",
        "program": "${workspaceFolder}/build/apps/ray-tracer",
        "args": [ "-f", "png", "-o", "renders/spheres.png", "-r", "1920x1080", "-s", "sphere" ],
        "environment": [{ "name": "config", "value": "Debug" }],
        "cwd": "${workspaceFolder}",
        "preLaunchTask": "CMake: build debug"
      }
    ]
}